import { createFileRoute, Link, useNavigate } from "@tanstack/react-router";
import { useEffect, useState } from "react";
import { BookOpen, CalendarCheck, Clock3, Crown, GraduationCap, LogOut, PlayCircle, Settings2, UserRound } from "lucide-react";
import { supabase } from "@/integrations/supabase/client";
import { Button } from "@/components/ui/button";
export const Route=createFileRoute("/dashboard")({ssr:false,head:()=>({meta:[{title:"Learning Dashboard | CatchUp Tutors"},{name:"description",content:"Manage your CatchUp Tutors learning, bookings, resources, and premium access."}]}),component:Dashboard});
function Dashboard(){const navigate=useNavigate();const [name,setName]=useState("Student");useEffect(()=>{supabase.auth.getUser().then(({data})=>{if(!data.user){navigate({to:"/auth",replace:true});return}setName(String(data.user.user_metadata.full_name||data.user.email||"Student"))})},[navigate]);async function logout(){await supabase.auth.signOut();navigate({to:"/auth",replace:true})}return <div className="min-h-screen bg-background"><header className="border-b bg-card"><div className="mx-auto flex h-20 max-w-7xl items-center justify-between px-4 sm:px-6 lg:px-8"><Link to="/" className="flex items-center gap-2 font-display font-bold"><GraduationCap className="text-primary"/> CatchUp Tutors</Link><Button variant="ghost" onClick={logout}><LogOut/> Sign out</Button></div></header><main className="mx-auto max-w-7xl px-4 py-10 sm:px-6 lg:px-8"><p className="text-sm font-bold uppercase tracking-widest text-primary">Student dashboard</p><h1 className="mt-2 font-display text-4xl font-bold">Welcome back, {name.split(" ")[0]}.</h1><p className="mt-3 text-muted-foreground">Here’s your learning control center.</p><div className="mt-10 grid gap-5 md:grid-cols-4">{[[CalendarCheck,"0","Upcoming sessions"],[Crown,"Free","Current plan"],[BookOpen,"0","Saved resources"],[Clock3,"0h","Learning this week"]].map(([Icon,value,label])=><article key={String(label)} className="rounded-2xl border bg-card p-5 shadow-soft"><Icon className="text-primary"/><p className="mt-5 font-display text-3xl font-bold">{String(value)}</p><p className="text-sm text-muted-foreground">{String(label)}</p></article>)}</div><div className="mt-8 grid gap-6 lg:grid-cols-[1.4fr_.6fr]"><section className="rounded-3xl border bg-card p-7"><div className="flex items-center justify-between"><h2 className="font-display text-xl font-bold">Continue learning</h2><Button asChild variant="link"><Link to="/resources">Browse library</Link></Button></div><div className="mt-8 rounded-2xl border border-dashed p-10 text-center"><PlayCircle className="mx-auto h-10 w-10 text-muted-foreground"/><h3 className="mt-4 font-semibold">Your recent topics will appear here</h3><p className="mt-2 text-sm text-muted-foreground">Open a topic or lesson to start tracking progress.</p></div></section><aside className="rounded-3xl bg-brand-navy p-7 text-hero-foreground"><Crown className="text-brand-orange"/><h2 className="mt-5 font-display text-2xl font-bold">Go deeper with Premium</h2><p className="mt-3 text-sm leading-6 text-hero-foreground/65">Unlock full video lessons, worked examples, and premium resources.</p><Button asChild variant="hero" className="mt-6 w-full"><Link to="/pricing">View plans</Link></Button></aside></div><div className="mt-8 grid gap-5 md:grid-cols-3">{[[UserRound,"Profile overview"],[CalendarCheck,"Booking history"],[BookOpen,"Downloads & saved resources"]].map(([Icon,label])=><div key={String(label)} className="flex items-center gap-4 rounded-2xl border bg-card p-5"><Icon className="text-primary"/><span className="font-semibold">{String(label)}</span></div>)}</div></main></div>}