import { createFileRoute, Link, useNavigate } from "@tanstack/react-router";
import { useState } from "react";
import { z } from "zod";
import { ArrowLeft, Eye, EyeOff, GraduationCap } from "lucide-react";
import { supabase } from "@/integrations/supabase/client";
import { lovable } from "@/integrations/lovable";
import { Button } from "@/components/ui/button";
import logo from "@/assets/catchup-logo.png.asset.json";

export const Route=createFileRoute("/auth")({head:()=>({meta:[{title:"Sign In or Register | CatchUp Tutors"},{name:"description",content:"Access your CatchUp Tutors learning dashboard or create a student or tutor account."},{property:"og:title",content:"Join CatchUp Tutors"},{property:"og:description",content:"Create your CatchUp learning account."}]}),component:AuthPage});
const schema=z.object({name:z.string().trim().min(2).max(100).optional(),email:z.string().trim().email().max(255),password:z.string().min(8).max(72)});
function AuthPage(){const [mode,setMode]=useState<"login"|"register">("login");const [show,setShow]=useState(false);const [busy,setBusy]=useState(false);const [message,setMessage]=useState("");const navigate=useNavigate();async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setMessage("");const form=new FormData(e.currentTarget);const parsed=schema.safeParse({name:String(form.get("name")||"")||undefined,email:String(form.get("email")),password:String(form.get("password"))});if(!parsed.success){setMessage(parsed.error.issues[0]?.message||"Check your details.");return}setBusy(true);if(mode==="login"){const {error}=await supabase.auth.signInWithPassword({email:parsed.data.email,password:parsed.data.password});if(error)setMessage(error.message);else navigate({to:"/dashboard"});}else{const {data,error}=await supabase.auth.signUp({email:parsed.data.email,password:parsed.data.password,options:{emailRedirectTo:window.location.origin,data:{full_name:parsed.data.name}}});if(error)setMessage(error.message);else if(data.user){await supabase.from("profiles").insert({id:data.user.id,email:parsed.data.email,full_name:parsed.data.name||""});await supabase.from("user_roles").insert({user_id:data.user.id,role:"student"});setMessage("Check your email to verify your account, then sign in.");}}setBusy(false)}async function google(){const result=await lovable.auth.signInWithOAuth("google",{redirect_uri:`${window.location.origin}/dashboard`,extraParams:{prompt:"select_account"}});if(result.error)setMessage(result.error.message)}return <div className="grid min-h-screen bg-background lg:grid-cols-2"><div className="hidden bg-hero p-12 text-hero-foreground lg:flex lg:flex-col"><Link to="/" className="flex items-center gap-2 text-sm text-hero-foreground/70"><ArrowLeft/> Back home</Link><div className="my-auto"><img src={logo.url} alt="CatchUp Tutors" className="w-72"/><h1 className="mt-10 font-display text-5xl font-bold">Catch Up.<br/><span className="text-brand-orange">Stay Ahead.</span></h1><p className="mt-6 max-w-md text-lg leading-8 text-hero-foreground/65">Your resources, tutoring, premium lessons, and progress—all in one focused learning space.</p></div></div><main className="flex items-center justify-center p-6"><div className="w-full max-w-md"><Link to="/" className="mb-8 flex items-center gap-2 lg:hidden"><ArrowLeft/> Home</Link><GraduationCap className="h-10 w-10 text-primary"/><h1 className="mt-5 font-display text-3xl font-bold">{mode==="login"?"Welcome back":"Create your account"}</h1><p className="mt-2 text-muted-foreground">{mode==="login"?"Continue your learning journey.":"Start as a student. Tutor applications follow after signup."}</p><Button variant="outline" size="lg" className="mt-8 w-full" onClick={google}>Continue with Google</Button><div className="my-6 flex items-center gap-4 text-xs text-muted-foreground"><span className="h-px flex-1 bg-border"/>OR<span className="h-px flex-1 bg-border"/></div><form onSubmit={submit} className="space-y-4">{mode==="register"&&<label className="grid gap-2 text-sm font-semibold">Full name<input name="name" required minLength={2} maxLength={100} className="h-12 rounded-xl border bg-card px-4"/></label>}<label className="grid gap-2 text-sm font-semibold">Email<input name="email" required type="email" maxLength={255} className="h-12 rounded-xl border bg-card px-4"/></label><label className="grid gap-2 text-sm font-semibold">Password<div className="flex rounded-xl border bg-card"><input name="password" required minLength={8} maxLength={72} type={show?"text":"password"} className="h-12 flex-1 bg-transparent px-4 outline-none"/><button type="button" onClick={()=>setShow(!show)} className="px-4" aria-label="Toggle password visibility">{show?<EyeOff/>:<Eye/>}</button></div></label>{message&&<p role="alert" className="rounded-xl bg-muted p-3 text-sm">{message}</p>}<Button type="submit" size="lg" className="w-full" disabled={busy}>{busy?"Please wait…":mode==="login"?"Sign in":"Create account"}</Button></form><button onClick={()=>{setMode(mode==="login"?"register":"login");setMessage("")}} className="mt-6 w-full text-center text-sm font-semibold text-primary">{mode==="login"?"New here? Create an account":"Already have an account? Sign in"}</button>{mode==="login"&&<button className="mt-4 w-full text-center text-sm text-muted-foreground">Forgot password?</button>}</div></main></div>}